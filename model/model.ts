//modelo do banco de dados para receber o arquivo -->nao sei se e o mesmo de transformar o arquivo em json

const createDatabase = {
      
}

const arquivo  = { 
    user: 'thiago',
    password : 'key',
    localhost: 'https://localhost:2010',
}

const user = {
    index:(req,res) =>{
        const server = async(await)
    },

    createUser (name,password,email) {
       this.password = password;
       this.email = email;
       this.name = name;
    
       if(password != password ){

       }

    }
}